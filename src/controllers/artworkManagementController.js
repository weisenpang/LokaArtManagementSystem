
//approve and reject artwork (staff, admin)

export const approveArtwork = async (req,res) => {
    res.status(200).json({message : "approve artwork under construction 😢"})
}

export const deleteUser = async (req,res) => {
    res.status(200).json({message : "reject artwork under construction 😢"})
}