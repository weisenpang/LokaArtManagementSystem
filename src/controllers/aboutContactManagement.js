//edit about us, edit contact us (admin)

export const editAboutUs = async (req,res) => {
    res.status(200).json({message : "edit about us under construction 😢"})
}

export const deleteAboutUs = async (req,res) => {
    res.status(200).json({message : "edit contact us under construction 😢"})
}