//edit , delete user (stadd,admin)

export const editUser = async (req,res) => {
    res.status(200).json({message : "edit user under construction 😢"})
}

export const deleteUser = async (req,res) => {
    res.status(200).json({message : "delete user under construction 😢"})
}